<%= form_for(@address_catalog) do |f| %>
  <% if @address_catalog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@address_catalog.errors.count, "error") %> prohibited this address_catalog from being saved:</h2>

      <ul>
      <% @address_catalog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

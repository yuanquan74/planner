<h1>Listing trip_states</h1>

<table>
  <tr>
    <th>State</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @trip_states.each do |trip_state| %>
  <tr>
    <td><%= trip_state.state %></td>
    <td><%= link_to 'Show', trip_state %></td>
    <td><%= link_to 'Edit', edit_trip_state_path(trip_state) %></td>
    <td><%= link_to 'Destroy', trip_state, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Trip state', new_trip_state_path %>
